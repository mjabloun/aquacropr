% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_irr.R
\name{write_irr}
\alias{write_irr}
\title{Write an AquaCrop irrigation file (\code{.IRR})}
\usage{
write_irr(
  path,
  mode,
  method = "sprinkler",
  surface_wetted = NULL,
  description = NULL,
  version = "7.3",
  version_note = "July 2026",
  events = NULL,
  reference_day = -9,
  time_criterion = 1L,
  depth_criterion = 1L,
  rules = NULL,
  raw_depletion = 30
)
}
\arguments{
\item{path}{Destination \code{.IRR}.}

\item{mode}{\code{1}/\code{"events"}, \code{2}/\code{"generate"}, or \code{3}/\code{"net"}.}

\item{method}{Irrigation method \code{1:5} or \code{"sprinkler"}, \code{"basin"},
\code{"border"}, \code{"furrow"}, \code{"drip"}. Default sprinkler.}

\item{surface_wetted}{Percent of soil surface wetted. Default depends on
\code{method} (100 / 100 / 100 / 90 / 30).}

\item{description}{File title (line 1).}

\item{version, version_note}{AquaCrop version line.}

\item{events}{For \code{mode = 1}: data.frame with \code{day} (days after planting,
or after \code{reference_day}), \code{depth} (mm), optional \code{ecw} (dS/m).}

\item{reference_day}{For \code{mode = 1}: AquaCrop day number of Day 1, a
\code{Date}, or \code{-9} (default) for the start of the growing period.}

\item{time_criterion}{For \code{mode = 2}: \code{1} fixed interval, \code{2} depletion
(mm), \code{3} depletion (\% RAW), \code{4} minimum bund water. Names
\code{"interval"}, \code{"depletion_mm"}, \code{"depletion_raw"}, \code{"bund"} also work.}

\item{depth_criterion}{For \code{mode = 2}: \code{1} back to field capacity, \code{2}
fixed net depth. Names \code{"field_capacity"} / \code{"fixed"}.}

\item{rules}{For \code{mode = 2}: data.frame with \code{from_day}, \code{time_value},
\code{depth_value}, optional \code{ecw}. First \code{from_day} must be 1.}

\item{raw_depletion}{For \code{mode = 3}: allowable RAW depletion (\%) so the
root zone does not drop below this threshold (0 = field capacity).}
}
\value{
\code{path}, invisibly.
}
\description{
FAO Table 2.23q-1. Three modes:
}
\details{
\enumerate{
\item \strong{events} (\code{mode = 1}): specified irrigation days, depths, and ECw
\item \strong{generate} (\code{mode = 2}): rules that AquaCrop uses to schedule irrigations
\item \strong{net} (\code{mode = 3}): net irrigation requirement for a RAW depletion
}

Rainfed cropping is \code{(None)} in the project file, not an \code{.IRR}.
}
\examples{
\dontrun{
write_irr("DATA/Inet.IRR", mode = "net", raw_depletion = 30)
write_irr("DATA/Igen.IRR", mode = "generate",
             time_criterion = "depletion_raw",
             depth_criterion = "field_capacity",
             rules = data.frame(from_day = 1, time_value = 80, depth_value = 0))
}
}
\seealso{
\code{\link[=write_project]{write_project()}}
}
