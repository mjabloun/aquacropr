% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objective.R
\name{compute_generic_objective}
\alias{compute_generic_objective}
\title{Compare simulated AquaCrop output to observed data}
\usage{
compute_generic_objective(
  obs_data,
  sim_file,
  value_map,
  join_cols = "Date",
  sim_reader = read_out,
  sim_data = NULL,
  sim_mutator = NULL,
  metric = c("RMSE", "KGE", "MAE"),
  metric_fun = NULL,
  weights = NULL,
  return_per_pair = FALSE,
  plot = FALSE,
  plot_labels = list(x = "Observed", y = "Simulated")
)
}
\arguments{
\item{obs_data}{data.frame of observed values. Must contain \code{join_cols}
plus every \code{obs_col} in \code{value_map}.}

\item{sim_file}{Optional path to an AquaCrop \code{.OUT} file.}

\item{value_map}{List/data.frame of observed vs simulated column pairs.}

\item{join_cols}{Key column(s) (default \code{"Date"}).}

\item{sim_reader}{Function that reads \code{sim_file}. Defaults to \code{\link[=read_out]{read_out()}}.}

\item{sim_data}{Optional already-loaded table (e.g. from \code{\link[=read_out]{read_out()}}).}

\item{sim_mutator}{Optional function applied after reading.}

\item{metric}{One of \code{"RMSE"}, \code{"KGE"}, \code{"MAE"}. Ignored if \code{metric_fun}
is supplied.}

\item{metric_fun}{Optional \code{f(sim, obs)} returning a scalar.}

\item{weights}{Numeric vector, one per \code{value_map} row.}

\item{return_per_pair}{If \code{TRUE}, also return per-pair scores.}

\item{plot}{If \code{TRUE}, build scatter plots (requires ggplot2).}

\item{plot_labels}{List with \code{x}/\code{y} labels.}
}
\value{
A list with \code{summary} and optionally \code{per_variable} and \code{plot}.
}
\description{
Join observed and simulated tables on shared key column(s), score one or
more column pairs with a goodness-of-fit metric, and combine them into a
single weighted score.
}
