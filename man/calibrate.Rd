% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{calibrate}
\alias{calibrate}
\title{Calibrate AquaCrop parameters against observations}
\usage{
calibrate(
  parameters,
  objective,
  sim_file,
  aquacrop_exe = NULL,
  working_dir = NULL,
  template_dir = ".",
  output_dir = ".",
  show_log = FALSE,
  maximize = FALSE,
  method = "DEoptim",
  control = list(),
  reporter = NULL,
  cmd = NULL,
  names = NULL,
  progress_window = FALSE
)
}
\arguments{
\item{parameters}{An \code{aquacropr_parameters} object.}

\item{objective}{An objective from \code{\link[=objective_spec]{objective_spec()}}.}

\item{sim_file}{Path to the \code{.OUT} file scored after the run (relative
to \code{working_dir} if that is set).}

\item{aquacrop_exe, working_dir, show_log, cmd, progress_window}{Passed to
\code{\link[=run_aquacrop]{run_aquacrop()}}.}

\item{template_dir, output_dir}{Passed to \code{\link[=render_templates]{render_templates()}}.}

\item{maximize}{If \code{TRUE}, return \code{-summary} so a minimizer can maximize
a metric such as KGE.}

\item{method}{\code{"DEoptim"} (default), \code{"BOBYQA"}, \code{"multi-BOBYQA"},
\code{"DDS"}, or \code{"CMA-ES"}.}

\item{control}{Method-specific list (see above).}

\item{reporter}{Optional function called after every evaluation. It
receives \code{evals}, \code{iter}, \code{np}, \code{n_total}, \code{generation_end},
\code{last_score}, \code{best_score}, and \code{best_params} (full named vector).}

\item{names}{Names to vary. Others stay at defaults. \code{NULL} means all.}
}
\value{
A list with \code{best_params}, \code{fitted_names}, \code{best_score}, \code{method},
and \code{fit} (the raw optimizer object).
}
\description{
Fits \code{names} (or every name in \code{\link[=parameter_names]{parameter_names()}}) against
\code{\link[=evaluate_candidate]{evaluate_candidate()}}. Bounds come from \code{min} / \code{max}. Methods:
}
\details{
\describe{
\item{\code{DEoptim}}{Differential evolution (\code{DEoptim}). Default. Control:
\code{NP}, \code{itermax}, plus other \code{DEoptim.control()} keys.}
\item{\code{BOBYQA}}{Bound-constrained local search (\code{minqa::bobyqa}).
Good as a polish after screening. Control: \code{maxfun} / \code{maxeval},
\code{npt}, \code{rhobeg}, \code{rhoend}, \code{start}.}
\item{\code{multi-BOBYQA}}{BOBYQA from several starts. The first start is
\code{control$start} (or the defaults) unless \code{include_default = FALSE}.
The rest are an LHS (or uniform) sample in the box. Control:
\code{nstarts} (default 8), \code{seed}, \code{include_default}, plus BOBYQA keys.}
\item{\code{DDS}}{Dynamically Dimensioned Search (Tolson & Shoemaker 2007),
implemented in the package. Control: \code{maxeval} (default 200),
\code{r} (default 0.2), \code{seed}, \code{start}.}
\item{\code{CMA-ES}}{Covariance matrix adaptation (\code{cmaes::cma_es}).
Control: \code{maxit}, \code{maxeval} (used to set \code{maxit} if omitted),
\code{sigma}, \code{start}, plus other \code{cma_es} control keys.}
}

Pass \code{names} to fit a subset (typical after Morris / Sobol); the rest
stay at defaults. \code{control$start} is the initial vector for BOBYQA, DDS,
and CMA-ES (defaults, clipped inside the box).
}
