% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{evaluate_candidate}
\alias{evaluate_candidate}
\title{One calibration iteration: render templates, run AquaCrop, score}
\usage{
evaluate_candidate(
  p,
  parameters,
  objective,
  sim_file,
  aquacrop_exe = NULL,
  working_dir = NULL,
  template_dir = ".",
  output_dir = ".",
  show_log = FALSE,
  maximize = FALSE,
  cmd = NULL,
  names = NULL,
  progress_window = FALSE
)
}
\arguments{
\item{p}{Numeric vector of values, in the order of \code{names} (default:
every name in \code{\link[=parameter_names]{parameter_names()}}).}

\item{parameters}{An \code{aquacropr_parameters} object.}

\item{objective}{An objective from \code{\link[=objective_spec]{objective_spec()}}.}

\item{sim_file}{Path to the \code{.OUT} file scored after the run (relative
to \code{working_dir} if that is set).}

\item{aquacrop_exe, working_dir, show_log, cmd, progress_window}{Passed to
\code{\link[=run_aquacrop]{run_aquacrop()}}.}

\item{template_dir, output_dir}{Passed to \code{\link[=render_templates]{render_templates()}}.}

\item{maximize}{If \code{TRUE}, return \code{-summary} so a minimizer can maximize
a metric such as KGE.}

\item{names}{Names to vary. Others stay at defaults. \code{NULL} means all.}
}
\value{
Numeric scalar: the objective summary (or its negation).
}
\description{
Building block of \code{\link[=calibrate]{calibrate()}}. Call it directly to score one candidate
or the best-fit vector from an optimizer.
}
