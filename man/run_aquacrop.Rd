% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{run_aquacrop}
\alias{run_aquacrop}
\title{Run the AquaCrop stand-alone plugin}
\usage{
run_aquacrop(
  working_dir = NULL,
  aquacrop_exe = NULL,
  show_log = FALSE,
  progress_window = FALSE,
  program_parameters = TRUE,
  cmd = NULL
)
}
\arguments{
\item{working_dir}{Character scalar. Directory containing \verb{LIST/},
\verb{SIMUL/}, and \verb{OUTP/}. For AquaCrop 7.3 this must be the folder that
holds \code{AquaCrop73.exe} (the plugin ignores other working directories).
Defaults to \code{dirname(get_aquacrop_path())}.}

\item{aquacrop_exe}{Character scalar path to the executable, or \code{NULL}
(default) to use \code{\link[=get_aquacrop_path]{get_aquacrop_path()}}. Ignored when \code{cmd} is set and
does not contain \code{{aquacrop_exe}}.}

\item{show_log}{If \code{TRUE}, stream console output; otherwise it is
captured/discarded.}

\item{progress_window}{If \code{FALSE} (default), suppress AquaCrop 7.3's Tk
progress window. FAO's plugin already runs headless when \verb{import tkinter} fails; we force that by briefly renaming the bundled
\verb{_internal/_tkinter.pyd} next to the executable (restored when the
call finishes). Set \code{TRUE} to keep the official progress dialog.}

\item{program_parameters}{If \code{TRUE} (default), AquaCrop may load sibling
\code{.PP1} / \code{.PPn} files next to each project in \verb{LIST/} (same basename;
they are not listed in the \code{.PRM}). If \code{FALSE}, those files are
temporarily renamed for the duration of the call so the run uses FAO
Table 2.19a defaults. Write custom files with \code{\link[=write_pp]{write_pp()}}.}

\item{cmd}{Optional command template, interpolated with \code{\link[glue:glue]{glue::glue()}}.
Substitutions: \code{{aquacrop_exe}}, \code{{working_dir}}. If \code{NULL}, runs
\code{"{aquacrop_exe}"}.}
}
\value{
Integer exit/status code from \code{\link[=system]{system()}} (invisibly).
}
\description{
Thin wrapper around \code{\link[=system]{system()}}. The FAO plugin does \strong{not} take a project
file on the command line: it must be launched from a working directory
that already contains \code{LIST/ListProjects.txt}, \verb{LIST/*.PRM} (or \code{.PRO}),
\verb{SIMUL/}, and \verb{OUTP/} (see \code{\link[=prepare_plugin_workdir]{prepare_plugin_workdir()}}).
}
\seealso{
\code{\link[=set_aquacrop_path]{set_aquacrop_path()}}, \code{\link[=prepare_plugin_workdir]{prepare_plugin_workdir()}}, \code{\link[=write_pp]{write_pp()}}
}
