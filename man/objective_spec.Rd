% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objective.R
\name{objective_spec}
\alias{objective_spec}
\title{Define a reusable objective specification}
\usage{
objective_spec(
  obs_data,
  value_map,
  join_cols = "Date",
  sim_reader = read_out,
  sim_mutator = NULL,
  metric = c("RMSE", "KGE", "MAE"),
  metric_fun = NULL,
  weights = NULL,
  output_file = NULL
)
}
\arguments{
\item{obs_data}{data.frame of observed values. Must contain \code{join_cols}
plus every \code{obs_col} in \code{value_map}.}

\item{value_map}{List/data.frame of observed vs simulated column pairs.}

\item{join_cols}{Key column(s) (default \code{"Date"}).}

\item{sim_reader}{Function that reads \code{sim_file}. Defaults to \code{\link[=read_out]{read_out()}}.}

\item{sim_mutator}{Optional function applied after reading.}

\item{metric}{One of \code{"RMSE"}, \code{"KGE"}, \code{"MAE"}. Ignored if \code{metric_fun}
is supplied.}

\item{metric_fun}{Optional \code{f(sim, obs)} returning a scalar.}

\item{weights}{Numeric vector, one per \code{value_map} row.}

\item{output_file}{Optional basename of the \code{.OUT} file this objective
should read after a run (used by \code{\link[=composite_objective]{composite_objective()}}).}
}
\value{
A list of class \code{aquacropr_objective}.
}
\description{
Define a reusable objective specification
}
