% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_pp.R
\name{write_pp}
\alias{write_pp}
\title{Write an AquaCrop program-parameter file (\code{.PP1} or \code{.PPn})}
\usage{
write_pp(path, ..., params = NULL)
}
\arguments{
\item{path}{Destination \code{.PP1} / \code{.PPn}, or the matching \code{.PRO} / \code{.PRM}
(the extension is then derived).}

\item{...}{Named overrides of the 25 FAO program parameters (see Details).}

\item{params}{Optional named list of overrides (merged with \code{...}).}
}
\value{
The path actually written, invisibly.
}
\description{
FAO loads this file by \strong{basename} when a project is opened: \code{field.PRO}
looks for \code{field.PP1}, \code{field.PRM} looks for \code{field.PPn}, in the same
folder. It is \strong{not} named inside the project file. If the sibling is
missing, AquaCrop uses the Table 2.19a defaults (the same numbers this
helper writes unless you override them).
}
\details{
Parameter names and FAO defaults:
\itemize{
\item \code{evap_decline} (4), \code{ke_max} (1.10), \code{hi_cc_threshold} (5)
\item \code{root_expand_start} (70), \code{root_expand_max} (5.00), \code{root_stress_shape} (-6)
\item \code{germination_taw} (20), \code{p_eto_adj} (1.0), \code{aeration_days} (3)
\item \code{senescence_exp} (1.00), \code{psen_decrease} (12), \code{topsoil_cm} (10)
\item \code{evap_extract_cm} (30), \code{cn_depth_m} (0.30), \code{cn_amc} (1)
\item \code{salt_diffusion} (20), \code{salt_solubility} (100), \code{capillary_shape} (16)
\item \code{default_tmin} (12.0), \code{default_tmax} (28.0), \code{gdd_method} (3)
\item \code{rain_procedure} (1), \code{rain_effective_pct} (70), \code{rain_showers} (2),
\code{rain_evap_param} (5)
}
}
\seealso{
\code{\link[=write_project]{write_project()}}, \code{\link[=run_aquacrop]{run_aquacrop()}}
}
