% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{calibrate_ego}
\alias{calibrate_ego}
\title{Surrogate-assisted calibration via Efficient Global Optimization}
\usage{
calibrate_ego(
  metamodel,
  parameters,
  objective,
  sim_file,
  nsteps = 10,
  aquacrop_exe = NULL,
  working_dir = NULL,
  template_dir = ".",
  output_dir = ".",
  show_log = FALSE,
  maximize = FALSE,
  control = NULL,
  kmcontrol = NULL,
  cmd = NULL,
  progress_window = FALSE
)
}
\arguments{
\item{metamodel}{An \code{aquacropr_metamodel} from \code{\link[=fit_metamodel]{fit_metamodel()}}.}

\item{parameters}{An \code{aquacropr_parameters} object.}

\item{objective}{An objective from \code{\link[=objective_spec]{objective_spec()}}.}

\item{sim_file}{Path to the \code{.OUT} file scored after the run (relative
to \code{working_dir} if that is set).}

\item{nsteps}{Number of real AquaCrop evaluations.}

\item{aquacrop_exe, working_dir, show_log, cmd, progress_window}{Passed to
\code{\link[=run_aquacrop]{run_aquacrop()}}.}

\item{template_dir, output_dir}{Passed to \code{\link[=render_templates]{render_templates()}}.}

\item{maximize}{If \code{TRUE}, return \code{-summary} so a minimizer can maximize
a metric such as KGE.}

\item{control, kmcontrol}{Forwarded to \code{DiceOptim::EGO.nsteps()}.}
}
\value{
A list with \code{best_params}, \code{best_score}, \code{fit}, and a refit
\code{metamodel}.
}
\description{
Each of \code{nsteps} iterations evaluates one real AquaCrop candidate via
\code{\link[=evaluate_candidate]{evaluate_candidate()}} and refits the Kriging model.
}
