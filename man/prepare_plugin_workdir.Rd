% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workspace.R
\name{prepare_plugin_workdir}
\alias{prepare_plugin_workdir}
\title{Assemble a stand-alone plugin working directory}
\usage{
prepare_plugin_workdir(
  path,
  projects,
  simul_dir = NULL,
  overwrite = TRUE,
  daily_results = c(1L, 2L, 7L),
  relativize_to = NULL,
  program_parameters = TRUE
)
}
\arguments{
\item{path}{Character scalar. Directory to create (or reuse).}

\item{projects}{Character vector of \code{.PRO} / \code{.PRM} file paths to copy
into \verb{LIST/}.}

\item{simul_dir}{Character scalar path to a \code{SIMUL} folder, or \code{NULL}
to use \code{\link[=get_simul_dir]{get_simul_dir()}}.}

\item{overwrite}{If \code{TRUE} (default), replace \verb{LIST/}, \verb{SIMUL/}, and
\verb{OUTP/} inside \code{path}.}

\item{daily_results}{Integer vector of DailyResults.SIM codes to write
into the copied \verb{SIMUL/} (see \code{\link[=write_daily_results_sim]{write_daily_results_sim()}}). \code{NULL}
leaves the copied file unchanged. Default writes water balance (1),
crop (2), and climate (7) daily files.}

\item{relativize_to}{Directory that AquaCrop prepends onto every path
in the project file (the folder containing \code{AquaCrop73.exe}). The
v7.3 Python plugin concatenates that folder with the directory lines
in \code{.PRM}/\code{.PRO} files, so absolute Windows paths break. Pass \code{FALSE}
to leave paths unchanged. Default: directory of \code{\link[=get_aquacrop_path]{get_aquacrop_path()}}
when the executable can be found.}

\item{program_parameters}{If \code{TRUE} (default), also copy a sibling
\code{.PP1} / \code{.PPn} when one sits next to a project being copied.}
}
\value{
\code{path}, invisibly.
}
\description{
The FAO executable is started with no arguments and looks for
\code{LIST/ListProjects.txt} in the current directory. This helper copies a
\verb{SIMUL/} tree, copies one or more \code{.PRO}/\code{.PRM} files into \verb{LIST/},
writes \code{ListProjects.txt}, and creates empty \verb{OUTP/}.
}
\details{
Crop, soil, and climate files are \strong{not} copied: the project file
already stores their directories. Point those paths at files that exist
on disk (or copy them yourself into \code{path}).
}
