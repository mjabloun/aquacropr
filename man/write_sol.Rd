% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_soil.R
\name{write_sol}
\alias{write_sol}
\title{Write an AquaCrop soil profile file (\code{.SOL})}
\usage{
write_sol(
  path,
  layers,
  description = NULL,
  cn = NULL,
  rew = NULL,
  version = "7.3",
  version_note = "July 2026"
)
}
\arguments{
\item{path}{Destination \code{.SOL}.}

\item{layers}{A data.frame with one row per horizon. Required columns:
\code{thickness} (m), \code{sat}, \code{fc}, \code{wp} (vol \%), \code{ksat} (mm/day). Optional:
\code{penetrability} (default 100), \code{gravel} (default 0), \code{description},
\code{cra}, \code{crb} (computed when missing).}

\item{description}{File title (line 1).}

\item{cn}{Curve Number. Default from the top horizon's Ksat.}

\item{rew}{Readily evaporable water of the surface layer (mm, 0–15).
Default from the top horizon's FC and PWP.}

\item{version}{AquaCrop version token (default \code{"7.3"}).}

\item{version_note}{Text in parentheses after the version.}
}
\value{
\code{path}, invisibly.
}
\description{
FAO Table 2.23s-1 layout (AquaCrop 7.x): description, version, CN, REW,
number of horizons (max 5), unused \code{-9}, then one row per horizon with
thickness, θsat/θFC/θPWP, Ksat, penetrability, gravel, CRa, CRb, and a
label. If \code{cn} / \code{rew} / CRa / CRb are omitted they are filled with the
official defaults (CN from top-horizon Ksat; REW from top-horizon FC and
PWP; CRa/CRb from soil class + Ksat, Janssens 2006).
}
\examples{
\dontrun{
write_sol("DATA/field.SOL", data.frame(
  thickness = 4, sat = 46, fc = 31, wp = 15, ksat = 500,
  description = "loam"
))
}
}
\seealso{
\code{\link[=write_project]{write_project()}}
}
