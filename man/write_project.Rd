% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_project.R
\name{write_project}
\alias{write_project}
\title{Write an AquaCrop project file (\code{.PRO} or \code{.PRM})}
\usage{
write_project(
  path,
  crop,
  soil,
  sim_start = NULL,
  sim_end = NULL,
  crop_start = NULL,
  crop_end = NULL,
  climate = NULL,
  tnx = NULL,
  eto = NULL,
  plu = NULL,
  co2 = NULL,
  irrigation = NULL,
  management = NULL,
  calendar = NULL,
  groundwater = NULL,
  initial = NULL,
  offseason = NULL,
  observations = NULL,
  cultivation_year = 1L,
  seasons = NULL,
  description = "",
  version = "7.3",
  version_note = "July 2026",
  relativize_to = NULL,
  check = TRUE,
  program_parameters = FALSE
)
}
\arguments{
\item{path}{Destination \code{.PRO} or \code{.PRM}. If the extension is omitted,
\code{.PRO} is used for one run and \code{.PRM} for several.}

\item{crop}{Path to a \code{.CRO} file.}

\item{soil}{Path to a \code{.SOL} file.}

\item{sim_start, sim_end}{First and last day of the simulation period
(\code{Date}, or vectors recycled across seasons).}

\item{crop_start, crop_end}{First and last day of the growing cycle.
Default: the simulation period.}

\item{climate}{Optional path to a \code{.CLI} file. Temperature, ETo, rain,
and CO2 names are read from it unless you override them.}

\item{tnx, eto, plu, co2}{Optional paths that override the files named in
\code{climate}. \code{co2} defaults to \code{MaunaLoa.CO2} in \code{\link[=get_simul_dir]{get_simul_dir()}} when
the CLI names that file and it is not next to the \code{.CLI}.}

\item{irrigation, management, calendar, groundwater, initial, offseason, observations}{Optional paths to \code{.IRR}, \code{.MAN}, \code{.CAL}, \code{.GWT}, \code{.SW0}, \code{.OFF},
\code{.OBS}. \code{NULL} means FAO defaults (\code{(None)}).}

\item{cultivation_year}{Integer, usually \code{1} for annuals (seeding year).
Recycled across seasons.}

\item{seasons}{Optional data.frame with \code{sim_start} and \code{sim_end}, and
optionally \code{crop_start}, \code{crop_end}, \code{cultivation_year}. Overrides the
corresponding vector arguments.}

\item{description}{Project title (line 1). Empty string matches GUI
files that start with a blank line.}

\item{version}{AquaCrop version token (default \code{"7.3"}).}

\item{version_note}{Text in parentheses after the version
(default \code{"July 2026"}).}

\item{relativize_to}{Directory the plugin prefixes onto project paths
(the folder that contains \code{AquaCrop73.exe}). Default: directory of
\code{\link[=get_aquacrop_path]{get_aquacrop_path()}} when the executable is found. \code{FALSE} leaves
absolute paths.}

\item{check}{If \code{TRUE} (default), require that supplied files exist.}

\item{program_parameters}{If \code{FALSE} (default), do not write a sibling
\code{.PP1}/\code{.PPn}. \code{TRUE} writes FAO defaults via \code{\link[=write_pp]{write_pp()}}. A named
list is passed as overrides to \code{\link[=write_pp]{write_pp()}}.}
}
\value{
\code{path}, invisibly.
}
\description{
Builds a FAO stand-alone project from crop, soil, climate, dates, and
optional management files. A \strong{single} simulation is written as \code{.PRO}
(or \code{.PRM} if you choose that extension); \strong{several seasons} are written
as a multiple-run \code{.PRM}. The layout matches reference manual Table
2.23w-1: version and description, then for each run the cultivation year,
four AquaCrop day numbers, and the 14 input-file triplets (name +
directory). Missing optional files are written as \code{(None)}.
}
\details{
Directory lines are rewritten relative to \code{relativize_to} when that
argument is a folder (needed by AquaCrop 7.3, which concatenates the
executable directory onto every project path). Pass \code{FALSE} to keep
absolute paths, as the GUI does.
}
\examples{
\dontrun{
write_project(
  "LIST/field.PRM",
  crop = "DATA/WheatGDD.CRO",
  soil = "DATA/Silt.SOL",
  climate = "DATA/Tunis.CLI",
  sim_start = as.Date("1979-11-22"),
  sim_end   = as.Date("1980-05-23")
)
}
}
\seealso{
\code{\link[=prepare_plugin_workdir]{prepare_plugin_workdir()}}, \code{\link[=relativize_project_paths]{relativize_project_paths()}},
\code{\link[=write_cli]{write_cli()}}, \code{\link[=write_pp]{write_pp()}}, \code{\link[=aquacrop_day_number]{aquacrop_day_number()}}
}
